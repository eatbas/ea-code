[env]
# LLVM overrides removed — they referenced a non-existent installation and
# broke cc-rs (space in "Program Files" path). For aarch64 cross-compilation,
# use target-specific env vars instead:
# [target.aarch64-pc-windows-msvc.env]
# CC = "C:\\Program Files\\LLVM\\bin\\clang.exe"
# AR = "C:\\Program Files\\LLVM\\bin\\llvm-ar.exe"
